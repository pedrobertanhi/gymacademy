/**
 * Configurações do Spring.
 *
 * Beans, CORS, segurança, etc. Convenção: classes anotadas com
 * {@code @Configuration}.
 *
 * Configs esperadas:
 *   - SecurityConfig — autenticação/autorização
 *   - CorsConfig    — liberar origem do frontend
 *   - WebConfig     — converters, formatters, etc.
 */
package com.pi.projetoacademia.config;
