/**
 * Controllers REST.
 *
 * Recebem requisições HTTP do frontend e devolvem JSON.
 * Convenção: classes terminadas em {@code Controller}, anotadas com
 * {@code @RestController} e {@code @RequestMapping("/api/...")}.
 *
 * Exemplos esperados:
 *   - AlunoController
 *   - AdminController (login/cadastro de admin)
 *   - AulaController
 *   - CatracaController
 */
package com.pi.projetoacademia.controller;
